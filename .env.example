# Copy to .env in this directory, or put the same keys in ~/.config/tray/.env
# (loaded automatically on startup; real .env is gitignored).

TRAY_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
TRAY_SUPABASE_ANON_KEY=your-publishable-anon-key
