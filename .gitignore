# Local brainstorming / specs (not published); keep output-formats brainstorm in git
scratch/**
!scratch/output-formats.md

# Go build artifact when built from repo root
/tray

# Local env (secrets)
.env
.env.*
!.env.example

# Dolt database files (added by bd init)
.dolt/
*.db

# Go coverage (from `make test-cover` or `go test -coverprofile=...`)
coverage.out
coverage.html
